{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="block">
        <h2 class="pb-30">
            {{ form_start(form) }} 
            {{ form_rest(form) }}
            <button class="btn btn-primary">S'inscrire</button>
            <small>
                <a href="{{path('signin')}}">Vous avez déjà un compte ? </a>
            </small>
            {{ form_end(form) }}

        </h2>
    </div>
{% endblock %}
